<?php
require_once('sworder.php');

 class sword
{
public $name;
//леха запомни  $this->name = $name; - определяется является ли переменная того то объекта;
//$this это ССЫЛКА на ТЕКУЩИЙ объект и она нужна, чтобы обратиться к переменной в КОНТЕКСТЕ класса.

//леха запомни  $self->name = $name; -тоже самое ток для static;
	
public function animal($name)
{
$name=$_POST['play'];

     function __construct($name)
      {
      $this->name = $name;
      }

$stephan=$stephan.$name;
$way="D:/OSPanel/domains/sqlserver/вывод/".$stephan;
$arr=explode('/',$way);
$web=end($arr);
$web=reload($web);
	
}
}
$sword=new sword();
$sword->animal($name);
?>
<?php

red::zoo('users');

?> 